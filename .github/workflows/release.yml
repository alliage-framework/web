name: Release
on:
  push:
    branches:
      - main
      - beta
env:
  NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
jobs:
  build-and-test:
    uses: ./.github/workflows/build-and-test.yml
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
  release:
    runs-on: ubuntu-latest
    needs: ['build-and-test']
    environment: production
    steps:
      - uses: actions/checkout@v3
        with:
          persist-credentials: false
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: build
      - name: Unzip artifact
        run: |
          tar -xf build.tar.gz -C .
          rm -rf build.tar.gz
      - name: Authenticate and configure NPM registry
        run: |
          echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > .npmrc
          echo "registry=https://registry.npmjs.org/" >> .npmrc
          echo "always-auth=true" >> .npmrc
      - name: Create GitHub App Token
        uses: actions/create-github-app-token@v2
        id: app-token
        with:
          app-id: ${{ secrets.RELEASE_APP_CLIENT_ID }}
          private-key: ${{ secrets.RELEASE_APP_SECRET_KEY }}
      - name: Authenticate and configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git remote set-url origin https://x-access-token:${{ steps.app-token.outputs.token }}@github.com/${{ github.repository }}
      - name: Release
        run: |
          yarn prepare
          if [ "${{ github.ref }}" = "refs/heads/beta" ]
          then
            echo "Beta release:"
            yarn release --specifier prerelease --preid beta --yes
          else
            echo "Main release:"
            yarn release --yes
          fi
